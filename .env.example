# .env — ONLY secrets and host-local debug settings.
# Batch execution configuration lives in the batch YAML files.
# The runner propagates API keys automatically; everything else comes from batch config.

# OPENAI_API_KEY=sk-xxx
# GEMINI_API_KEY=xxx
# OPENCODE_GO_API_KEY=sk-...

# Optional host-local debug (does NOT propagate to containers)
# AUTOFIX_LOG_LEVEL=INFO

# ── run_final_experiment.py ─────────────────────────────────────────────────
# Topic for ntfy.sh push notifications (Ollama/local mode)
# NTFY_TOPIC=tfm-ollama

# Topic for ntfy.sh push notifications (GPT mode, --gpt-only)
# NTFY_TOPIC_GPT=tfm-gpt

# Minimum USD balance required before starting GPT runs (default: 1.0)
# OPENAI_MIN_BALANCE_USD=1.0
